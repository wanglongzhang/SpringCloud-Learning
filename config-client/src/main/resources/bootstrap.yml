spring:
    application:
        name: config-client
    cloud:
        config:
            uri: http://localhost:10020/
            profile: stage
            label: master
server:
    port: 10025

management:
    security:
        enabled: false